name: publish-download-center-entry-file

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/publish-download-entry.yml'
      - '00_실용음악분과_OS_공식다운로드센터.html'
  push:
    branches: [main]
    paths:
      - '.github/workflows/publish-download-entry.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate:
    name: Validate download-center entry file
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate portable HTML entry file
        run: |
          set -euo pipefail
          file='00_실용음악분과_OS_공식다운로드센터.html'
          test -f "$file"
          grep -q 'https://incheon-academy-union-os-web.vercel.app/download' "$file"
          grep -q 'app-icon.svg' "$file"
          grep -q '실용음악분과(임시) OS' "$file"
          grep -q '인천광역시학원연합회' "$file"

  publish:
    name: Publish entry file to v1.0.1 Release
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Upload as downloadable Release asset
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          file='00_실용음악분과_OS_공식다운로드센터.html'
          gh release upload v1.0.1 "$file" --clobber
          gh release view v1.0.1 --json assets --jq '.assets[].name' | grep -Fx "$file"
